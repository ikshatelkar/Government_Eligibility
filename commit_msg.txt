Add Top 5 schemes, fix eligibility filtering, and upgrade scheme data pipeline

- CheckEligibilityPage: show ranked Top 5 high-impact schemes with trophy UI;
  add "View all N eligible schemes" toggle for remaining results
- eligibility.controller: add state-based filtering (state-specific schemes now
  only shown to matching state users); strengthen age safety rules with category-
  level Education cap (>35), campus recruitment block (>40), apprenticeship (>35),
  child/maternity/senior citizen keyword guards
- fetch_schemes.py: rewritten to use MyScheme v4 API with x-api-key, incremental
  slug-based sync, and __NEXT_DATA__ page scraping (no Selenium)
- restore_schemes.py: rewritten to re-download pre-scraped CSV from GitHub for
  weekly full restore
- import_from_csv.py: new one-time import script that downloads and parses
  3,300+ schemes from Bhagawat8/Government-Scheme-QnA CSV into MySQL
